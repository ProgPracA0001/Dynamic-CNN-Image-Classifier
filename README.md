## Note
The original coursework implementation is included as a Jupyter notebook (`CNN_Original.ipynb`).  
The project has also been refactored into modular Python scripts (`model.py`, `train.py`) to reflect a more production-style structure.
